/*
This module would be responsible for reading data from MQTT
and providing it to other systems as a reactive stream
*/

/* STUB 
import Rx from 'rxjs';

import mqqtClient from '../mqqt-client/client';

const mqqtReadThread = Rx.from(function(cb){
  mqqtClient.onMessage(cb);
});

export default mqqtReadThread;

*/
